// Please bind your HTML forms using the @html.BeginForm() or @html.BeginRouteForm() helper extension, rather than tags with 'action' attributes.
window.addEventListener('pageshow', function() { Sys.Application.initialize(); });
